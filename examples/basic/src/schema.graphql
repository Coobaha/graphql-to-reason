type Article {
    id: String!
    title: String!
    body: String!
}

type Query {
    article(id: String!): Article
    articles: [Article!]!
}

type Mutation {
   addArticle(title: String!, body: String!): Article
}
